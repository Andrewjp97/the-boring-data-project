{
  "name": "site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "test:rules": "firebase emulators:exec --only firestore --project demo-recall-lookup \"vitest run test/rules.test.ts\"",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/node": "^9.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.15.0",
    "firebase-tools": "^15.22.4",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
