rules_version = '2';

// Deny ALL client access (SPEC §4). Only server-side service accounts
// (Cloud Run render path, ETL push) touch Firestore, and they bypass rules.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
